import { SplitQueryPipe } from './split-query.pipe';

describe('SplitQueryPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitQueryPipe();
    expect(pipe).toBeTruthy();
  });
});
